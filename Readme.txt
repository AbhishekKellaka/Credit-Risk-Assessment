1. Place loan.csv file from Dataset -> DataPrep_dataset and place it inside the Dataset folder

2. Install dependencies from requirements.txt file using the following command in windows command prompt:
	pip install -r requirements.txt
	
3. Run modelling.ipynb for model results.
