1. Extract loan.csv file from Dataset -> DataPrep_dataset and place it inside the Dataset folder
2. Install dependencies from requirements.txt file using command in windows command prompt:
	pip install -r requirements.txt